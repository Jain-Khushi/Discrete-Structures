#include<iostream>
#include<cstdlib>
using namespace std;
class SET
{
	public:
	     int A[10],B[10],C[20],m,n,i,j,k,choice,flag;
	  void operations()
	  {
	      cout<<"Enter the size of set A: ";
	     cin>>m;
	     cout<<"Enter the size of set B: ";
	     cin>>n;
	     cout<<"\nEnter the values of set A: ";
	     for(i=0;i<m;i++)
	        cin>>A[i];
	     cout<<"Enter the values of set B: ";
	     for(i=0;i<n;i++)
	         cin>>B[i];
		   
	do
                 {
	cout<<"\n\n...SET OPERATIONS...\n\n"
	      <<"1. Subset: Check whether one set is a subset of other or not.\n"
                        <<"2. Union and Intersection of two Sets.\n"
                        <<"3. Complement: Assume Universal Set as per the input elements from the user.\n"
                        <<"4. Set Difference and Symmetric Difference between two SETS.\n" 
                       <<"5. Cartesian Product of Sets.\n"
                      <<"6. Exit.\n";
                 cout<<"\nEnter your choice: ";
                cin>>choice;
            
                switch(choice)
                {
            	case 1:{int u=0;
	            for(i=0;i<m;i++)
                            {
            	           for(j=0;j<n;j++)
            	           {
            	           	  if(A[i]==B[j])
            	           	    ++u;
                             }
	       }
	        if(u==i)
	          cout<<"\nSet A is subset of B.\n";
	       else
	         cout<<"Set A is not the subset of B.\n";
						  
	     break;
	}
	case 2: cout<<"\nUnion of two sets is: ";
                              for(i=0;i<m;i++)
		cout<<A[i]<<" ";
	           for(j=0;j<n;j++)
		 cout<<B[j]<<" ";
				        
	          cout<<"\nIntersection of two sets is: ";
	         for(i=0;i<m;i++)
	             for(j=0;j<n;j++)
	            {
		if(A[i]==B[j])
		    cout<<A[i]<<" ";
                              }
                             break;
	  case 3: cout<<"\nUniversal set is: ";
	             for(i=0,k=0;i<m,k<(m+n);i++,k++)
		C[k]=A[i];
				              
	            for(j=0,k=m;j<n,k<(m+n);j++,k++)
		 C[k]=B[j];
				           
	           for(k=0;k<(m+n);k++)
		cout<<C[k]<<" ";
				             
	         cout<<"\nComplement of set A: ";
	         for(k=0;k<(m+n);k++)
	        {
	            int x=0;
	           for(i=0;i<m;i++)
	           {
		if(C[k]==A[i])
		   ++x;
                              }
	            if(x==0)
		cout<<C[k]<<" ";
	         }
	          cout<<"\nComplement of set B: ";
	          for(k=0;k<(m+n);k++)
	          {
	              int x=0;
	              for(j=0;j<n;j++)
	              {
		 if(C[k]==B[j])
		     ++x;
	               }
	              if(x==0)
		cout<<C[k]<<" ";
	           }
						
	          break;
	case 4: cout<<"\nSet difference:\n ";
	            cout<<"A-B: ";
				        
	           for(i=0;i<m;i++)
                             {
		int y=0;
		for(j=0;j<n;j++)
	                  {
		        if(A[i]==B[j])
		              ++y;
                                     }
                                     if(y==0)
                                         cout<<A[i]<<" ";
	              }
                                cout<<"\nB-A: ";
	              for(j=0;j<n;j++)
	              {
		int y=0;
		for(i=0;i<m;i++)
		{
		       if(B[j]==A[i])
		            ++y;
                                     }
                                    if(y==0)
		  cout<<B[j]<<" ";
	              }
		cout<<"\nSymmetric difference between A and B: ";
		for(i=0;i<m;i++)
		{
		      int z=0;
		      for(j=0;j<n;j++)
		      {
		             if(A[i]==B[j])
		                ++z;
                                            }
                                            if(z==0)
		          cout<<A[i]<<" ";
		  }
		for(j=0;j<n;j++)
		{
		     int z=0;
				           
                                        for(i=0;i<m;i++)
		 {
		          if(B[j]==A[i])
			++z;
                                        }
		 if(z==0)
		     cout<<B[j]<<" ";
	                   }
					
		break;
	       case 5: cout<<"\nThe cartesian product is: ";
		for(i=0;i<m;i++)
		     for(j=0;j<n;j++)
		         cout<<A[i]<<","<<B[j]<<"  ";
							  
		break; 
						   
	       case 6: exit(0);
            	
	}
               }while(1<=choice<=6);
		
     return ;}
	
};
int main()
{
	SET S;
	S.operations();
	return 0;
}