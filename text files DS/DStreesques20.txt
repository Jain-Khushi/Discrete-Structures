#include <iostream>
using namespace std;

int main()
{
    int internal,leaf,m;
    
    cout<<"Enter the 'm' in full m-ary tree : ";
    cin>>m;
    
    cout<<"Enter the number of internal vertices : ";
    cin>>internal;
    
    cout<<"The number of leaf nodes are : "<<(m-1)*internal + 1;
    return 0;
}